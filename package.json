{
  "name": "stratego",
  "version": "1.0.0",
  "description": "capture the flag",
  "main": "none",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel src/ --out-dir dist/ --ignore src/browser.jsx; browserify -t [ babelify ] src/browser.jsx -o dist/public/browser.js",
    "start": "node dist/server.js"
  },
  "author": "",
  "license": "ISC",
  "babel": {
    "presets": [
      "es2015",
      "react"
    ]
  },
  "dependencies": {
    "babel-cli": "^6.23.0",
    "babel-preset-es2015": "^6.22.0",
    "babel-preset-react": "^6.23.0",
    "babelify": "^7.3.0",
    "express": "^4.14.1",
    "react": "^15.4.2",
    "react-dom": "^15.4.2",
    "serve-favicon": "^2.4.0"
  }
}
